mutation activeWorkoutPlanCreate($input: ActiveWorkoutPlanInput!) {
    activeWorkoutPlanCreate(input: $input) {
        plan {
            id
        }
        errors {
            message
        }
    }
}
